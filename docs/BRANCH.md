# feature/firebase-backend

## What changed

Nothing in the app. This branch was opened off main so Firebase Authentication and Firestore could be wired to project `virtual-internship-b25d1`.

## Why

Sign-in and the library need a real project, and that work was meant to stay off the feature branches that were already merged.

## How

The branch was created and then paused before any code, rules, or env files were committed. The web app registration in the Firebase console is separate from this git history.
